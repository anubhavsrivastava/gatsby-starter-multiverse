import React from 'react';

import Layout from '../components/Layout';

const IndexPage = () => (
  <Layout>
    <section id="header">
      <header>
        <h2>NOT FOUND</h2>
      </header>
      <p>Not a valid URL</p>
    </section>
  </Layout>
);

export default IndexPage;
